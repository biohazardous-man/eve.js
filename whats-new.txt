remodeled config/index.js. 
made a universal start.bat for server, install.bat for npm install. 
updated files to use new database controller in the /database instead of /newDatabase. 
updated characterState.js to use the new database controller. 
moved shipTypes.json to static directory inside database folder + updated paths in other files. 
removed the feature where the server auto patches client, as it caused many unnecessary errors. 
